apiVersion: v1
kind: Service
metadata:
  name: {{ include "customApiSvc.fullname" . }}
  labels:
    {{- include "customApiSvc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "customApiSvc.selectorLabels" . | nindent 4 }}
